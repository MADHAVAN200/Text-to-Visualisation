{
  "name": "text-to-visualisation-workspace",
  "version": "1.0.0",
  "description": "Root workspace for Text to Visualisation",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "npm run dev --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "start": "concurrently -k -n \"backend,frontend\" -c \"yellow,blue\" \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
